services:
  - type: web
    name: anand-upscaler
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    buildCommand: ""
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
